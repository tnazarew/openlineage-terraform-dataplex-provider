# Build output
bin/
terraform-provider-openlineage

# Terraform working files
examples/.terraform/
examples/.terraform.lock.hcl
examples/terraform.tfstate
examples/terraform.tfstate.backup

# Local dev config — contains absolute paths, machine-specific
.terraformrc

# Scratchpad / outdated docs not relevant to this repo
ignored/

# macOS
.DS_Store

# IDE
.idea/

# go.work is machine-specific — points to a local clone of the OpenLineage monorepo
# for pre-publish development of github.com/OpenLineage/openlineage/byool/terraform.
# See go.work.example for setup instructions.
go.work
go.work.sum

# vendor/ is intentionally committed so the repo is self-contained.
# Do NOT add vendor/ here.

